---
/** Marque une donnée encore absente : pointillé accentué, et lisible par
    un lecteur d'écran. */
interface Props { label: string }
const { label } = Astro.props;
---
<span class="todo" title="Information à compléter">{label}</span>
