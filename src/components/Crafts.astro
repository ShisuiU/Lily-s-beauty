---
import { universes, practitioners, pseudo } from '../data/site';
const numerals = ['i', 'ii', 'iii'];
---
<section id="prestations">
  <div class="shell">
    <div class="duo">
      <div>
        <hr class="rule" />
        <h2 class="duo-lead">
          Un institut de village tenu par <em>deux praticiennes</em>, chacune sur sa spécialité.
        </h2>
        <p class="duo-body">
          Fiacrine s'occupe des ongles. Juliette des cils, des sourcils et de l'épilation.
          Chaque prestation est réservée pour sa durée réelle — dix minutes pour un menton,
          près de deux heures pour une pose complète — pour que personne ne soit expédié.
        </p>
        <ul class="who">
          {practitioners.map((p) => (
            <li>
              <b>{p.name}</b>
              <span>{p.craft}</span>
              {p.instagram && (
                <a class="who-ig" href={p.instagram} target="_blank" rel="noopener">
                  {pseudo(p.instagram)}
                </a>
              )}
            </li>
          ))}
        </ul>
      </div>
      <div class="crafts">
        {universes.map((u, i) => (
          <article class="craft">
            <span class="n" aria-hidden="true">{numerals[i]}</span>
            <div>
              <h3>{u.label}</h3>
              <p>{u.blurb}</p>
            </div>
          </article>
        ))}
      </div>
    </div>
  </div>
</section>
